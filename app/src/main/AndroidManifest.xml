<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_resiapp_logo"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@drawable/ic_resiapp_logo_wb"
        android:supportsRtl="true"
        android:theme="@style/Theme.ResiApp"
        tools:targetApi="31">
        <activity
            android:name="Activities.ReservationActivity"
            android:exported="false" />
        <activity
            android:name="Activities.ReviewActivity"
            android:exported="false" />
        <activity
            android:name="Activities.SpaceRuleActivity"
            android:exported="false" />
        <activity
            android:name="Activities.SpaceActivity"
            android:exported="false" />
        <activity
            android:name="Activities.UserActivity"
            android:exported="false" />
        <activity
            android:name="Activities.AdminDashboardActivity"
            android:exported="false" />
        <activity
            android:name="Activities.LoginActivity"
            android:exported="false" />
        <activity
            android:name="Activities.ResidentDashboardActivity"
            android:exported="false" />
        <activity
            android:name="Activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>